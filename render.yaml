services:
  - type: web
    name: dms-dashboard
    env: python
    buildCommand: "pip install -r requirements_server.txt"
    startCommand: "gunicorn wsgi:app -w 1 --threads 4"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
